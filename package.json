{
  "name": "stralls",
  "version": "1.0.0",
  "description": "Make API calls fast and simply.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cls && electron .",
    "package-win": "electron-packager . electron-tutorial-app --overwrite --no-asar --platform=win32 --arch=ia32 --icon=/assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Stralls\""
  },
  "keywords": [],
  "author": "Carlos Teixeira",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.7.9",
    "electron-packager": "^10.1.0"
  },
  "dependencies": {
    "axios": "^0.17.1"
  }
}
